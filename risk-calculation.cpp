#include "hash-map.cpp"
#include "graph-matrix.cpp"
/**
 * Generate the covid risk for each person
 */

Map<string, int> generateCovidRisk(GraphMatrix<string> graph)
{
    Map<string, int> risk;
    for (int i = 0; )
};